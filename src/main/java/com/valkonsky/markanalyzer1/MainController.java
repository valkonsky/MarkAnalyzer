package com.valkonsky.markanalyzer1;

import com.valkonsky.markanalyzer1.entity.School;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;
import javafx.event.ActionEvent;
import javafx.fxml.Initializable;
import javafx.scene.control.ListView;
import javafx.stage.FileChooser;
import javafx.stage.Stage;

import java.io.File;
import java.io.IOException;
import java.net.URL;
import java.util.ResourceBundle;

public class MainController {
/*
    ObservableList<String> schoolDataItems = FXCollections.observableArrayList();
    public ListView schoolData = new ListView(schoolDataItems);
    File file;
    public void uploadStartFile(ActionEvent actionEvent) {
        FileChooser fileChooser = new FileChooser();
        file = fileChooser.showOpenDialog(new Stage());

    }

    @Override
    public void initialize(URL location, ResourceBundle resources) {
        School school = null;
        try {
            school = new School("C:/Users/valko/Desktop/2412.xlsx");
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
        schoolDataItems.add("Кол-во классов: " + String.valueOf(school.getSchoolClasses().size()));
        schoolDataItems.add("Кол-во учеников: " + String.valueOf(school.getCountOfStudents()));
        schoolDataItems.add("Кол-во НАН: " + String.valueOf(school.getUnAttestedUnrespectedStudents().size()));
        schoolDataItems.add("Кол-во НАУ: " + String.valueOf(school.getUnAttestedRespectedStudents().size()));
        schoolDataItems.add("Кол-во без оценки: " + String.valueOf(school.getUnmarkedStudents().size()));
        System.out.println(school.getUnmarkedStudents());
        schoolData.getItems().addAll(schoolDataItems);
    }

    public void getAllClasses(ActionEvent actionEvent) {*/

    //}
}
